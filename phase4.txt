Phase 4. Extract the data layer.
- src/<pkg>/data/dataset.py: TemperatureSalinityDataset (~245-706), now consuming
  io.* and config. Keep reload(), get_profiles, calc_gem, get_gem_profiles, etc.
  exactly as-is behaviorally.
- src/<pkg>/data/pca.py: ONE canonical PCA fit + inverse_transform. There are 5
  inverse_transform variants (lines 514, 977, 1043, 2058, 2631). First write a
  test that pins each call site's current output; THEN implement a single function
  parameterized to cover them; THEN prove equivalence; THEN replace call sites.
  If any variant can't be unified safely, keep it separate and document why.
- src/<pkg>/data/splits.py: split_dataset + IndexedSubset.
- src/<pkg>/data/gem.py: GEM baseline if cleaner separated from the dataset.
Run the full characterization suite — this phase is the highest-risk one.