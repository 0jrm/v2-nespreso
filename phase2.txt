Phase 2. Centralize configuration. Do not move algorithmic code.
- Create src/<pkg>/config.py with frozen dataclasses for: ModelConfig
  (n_components, layers_config, dropout_prob, batch_size, depths, epochs,
  patience, lr, splits), InputParams (the timecos/.../ssh dict), DensityConfig
  (the density_penalty_config dict), and Paths (every /unity/... path: ARGO .mat,
  AVISO, SST, SSS, dataset pickle, checkpoints, legacy API model/pca, ISOP/glider
  files). Load from configs/default.yaml with env-var overrides; resolve paths
  through Paths so nothing is hardcoded in logic.
- Replace the three sys.path.insert/append hacks with real imports enabled by
  pyproject (or a documented editable install of the local deps).
- Replace the module-level global flags (load_trained_model, ensemble_models,
  load_dataset_file, gen_paula_profiles, debug, seed, n_runs, *_repeat_time) with
  config fields.
Keep a backward-compat shim so the original script still runs. Run tests.